import 'package:flutter/material.dart';

class AppColors {
  // Light theme colors - More vibrant
  static const Color primary = Color(0xFF4CAF50);      // Bright green
  static const Color secondary = Color(0xFF8BC34A);    // Light green
  static const Color accent = Color(0xFF2196F3);       // Blue accent
  
  static const Color background = Color(0xFFFFFFFF);   // Pure white
  static const Color surface = Color(0xFFF5F5F5);      // Light gray
  static const Color error = Color(0xFFE53935);        // Bright red
  
  static const Color textPrimary = Color(0xFF212121);  // Dark gray
  static const Color textSecondary = Color(0xFF757575); // Medium gray
  
  // Dark theme colors (for later)
  static const Color darkBackground = Color(0xFF121212);
  static const Color darkSurface = Color(0xFF1E1E1E);
  static const Color darkTextPrimary = Color(0xFFFFFFFF);
  static const Color darkTextSecondary = Color(0xFFB0B0B0);
}